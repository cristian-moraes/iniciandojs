let cor = prompt ("Insira uma cor")
let codigo = prompt ("insira um simbolo")
if (cor === "vermelho" && codigo === "X") {
    alert ("Cave um buraco");
} else if (cor === "azul" && codigo === "$") {
    alert ("Siga em frente")
} else if ( cor === "verde" && codigo === "*") {
    alert ("Suba em uma árvore")
} else if (cor === "amarelo" && codigo === "#") {
    alert ("Pare e descanse")
}